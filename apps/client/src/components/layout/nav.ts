/**
 * The nav moved from a floating bottom bar to an icon row in each page's
 * header. Flip this to false to put `BottomTabBar` back — routing is
 * identical either way, so it is a one-word revert.
 */
export const TOP_NAV = true;
